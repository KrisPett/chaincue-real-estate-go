# docker compose up -d
# docker exec -it postgres-monolith psql -U admin -d postgres -c "CREATE DATABASE \"chaincue-real-estate-db\";"

version: '3.9'
services:
  postgres:
    container_name: postgres-monolith
    image: postgres:16.0
    environment:
      POSTGRES_DB: chaincue-real-estate-db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
#    command: bash -c "psql -U admin -d postgres -c \"CREATE DATABASE \\\"chaincue-real-estate-db\\\";\""
    ports:
      - "5432:5432"

